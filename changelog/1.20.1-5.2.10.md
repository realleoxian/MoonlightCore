# MoonlightCore | 5.2.10

1. Small change to the Fabric's registry order (still vanilla goes the same order first) so the `RegistryCreationEvent` 
adds the new registries to that order and are invoked.

2. Added the `RegisterClientCommandEvent` to allow mods add their own commands to only the client side.