# MoonlightCore | 5.2.12

1. Classes like `NonnullSupplier`, `NonnullConsumer`, `NonnullFunction`, etc, and registry entry builders
were removed
2. `DeferredRegistrar` was simplified, now there is no callback system since there is no longer
builders that use them