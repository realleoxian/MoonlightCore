# Moonlight Core | 4.1.3

This is a small update that works on a change that had problems (the cause of the hotfix 4.1.2), a problem with event listener list.
This adds a SortedLinkedLists to register all the listeners instead of having a Map with the priorities and listeners. (Another fix in other words (?))